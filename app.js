function gameOfLife(grid) {
  // Your code here
}

// Example usage:
const currentState = [
  [0, 1, 0],
  [0, 1, 0],
  [0, 1, 0]
];

const nextState = gameOfLife(currentState);
console.log(nextState);
